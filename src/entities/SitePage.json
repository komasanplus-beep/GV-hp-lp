{
  "name": "SitePage",
  "type": "object",
  "properties": {
    "site_id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "slug": {
      "type": "string"
    },
    "page_type": {
      "type": "string",
      "enum": [
        "home",
        "about",
        "menu",
        "staff",
        "gallery",
        "access",
        "contact",
        "custom"
      ]
    },
    "page_category": {
      "type": "string",
      "enum": [
        "regular",
        "privacy",
        "security",
        "compliance"
      ],
      "default": "regular",
      "description": "ページカテゴリ（固定ページを区別するため）"
    },
    "is_system_page": {
      "type": "boolean",
      "default": false,
      "description": "システム生成ページか（手動削除対象外）"
    },
    "show_in_header": {
      "type": "boolean",
      "default": false,
      "description": "ヘッダーメニューに表示するか"
    },
    "show_in_footer": {
      "type": "boolean",
      "default": false,
      "description": "フッターに自動表示するか"
    },
    "menu_label": {
      "type": "string",
      "description": "メニュー表示時のラベル上書き（空白時はtitle使用）"
    },
    "sort_order": {
      "type": "number",
      "default": 0,
      "description": "メニュー内での表示順"
    },
    "parent_page_id": {
      "type": "string",
      "description": "親ページID（階層構造）"
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "published"
      ],
      "default": "draft"
    },
    "use_common_contact": {
      "type": "boolean",
      "default": true,
      "description": "共通連絡先設定を使用するか"
    },
    "contact_override": {
      "type": "object",
      "description": "共通設定の上書き用連絡先情報",
      "properties": {
        "shop_name": {
          "type": "string",
          "description": "店舗名（上書き用）"
        },
        "address": {
          "type": "string"
        },
        "phone": {
          "type": "string"
        },
        "email": {
          "type": "string"
        },
        "map_embed_url": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "site_id",
    "title",
    "slug"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "user_condition": {
        "role": "admin"
      }
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}